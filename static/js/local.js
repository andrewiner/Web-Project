'use strict';
// 可以直接用jQuery

/**
 * @param {number} item_id
 */
function collect(item_id)
{
    console.log('收藏' + item_id);
}

/**
 * @param {number} item_id
 */
function cancel_collect(item_id)
{
    console.log('取消收藏' + item_id);
}
